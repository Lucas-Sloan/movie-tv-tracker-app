<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard</title>
  <link rel="stylesheet" href="/css/nav.css">
  <link rel="stylesheet" href="/css/pages/dashboard.css">
</head>
<body>
  <%- include('../partials/nav') %>
  <header class="dashboard-header">
    <h1>MY BINDER</h1>
  </header>
  <nav class="dashboard-nav">
    <ul>
      <li><a href="/cinemas/dashboard">All Media</a></li>
      <li><a href="/cinemas/tvshows">TV Shows</a></li>
      <li><a href="/cinemas/movies"><strong>Movies</strong></a></li>
    </ul>
  </nav>
  <main class="dashboard-main">
    <h2>Movies</h2>
    <table>
      <thead>
        <tr>
          <th>#</th>
          <th>Title</th>
          <th>Actions</th>
          <th>Score</th>
          <th>Status</th>
        </tr>
      </thead>
      <tbody>
        <% if (movies.length > 0) { %>
          <% movies.forEach((movie, index) => { %>
            <tr>
              <td><%= index + 1 %></td>
              <td><a href="/cinemas/show/<%= movie._id %>"><%= movie.title %></a></td>
              <td><a href="/cinemas/editmovies/<%= movie._id %>">Edit</a></td>
              <td><%= movie.score %></td>
              <td><%= movie.status %></td>
            </tr>
          <% }) %>
        <% } else { %>
          <tr>
            <td colspan="5">You haven't added any Movies yet</td>
          </tr>
        <% } %>
      </tbody>
    </table>
  </main>
</body>
</html>